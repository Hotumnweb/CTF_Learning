"""
写函数 `add(a, b)`，返回两数之和
"""

def add(a,b):
    result = a + b
    return result
print(add(a=23, b=13))

"""
- `def` — 告诉 Python"我要定义一个函数了"（define 的缩写）
- `add` — 函数名，你自己起，调用时用这个名字
- `(a, b)` — 参数，函数接收的输入，相当于占位符
- `:` — 冒号不能少，表示下面是函数体
- 缩进（4 个空格）— 属于函数内部的代码必须缩进
- `return` — 把结果 "扔" 出来，函数外部才能拿到
"""