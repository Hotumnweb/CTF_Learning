# 你的现状梳理

> CTF：入门水平，只会少量题型；
>
> HTML：看得懂，写吃力；CSS：一知半解；JS：了解概念；
>
> PHP：只见过 CTF 题目里的零散语法，没有完整写过页面；
>
> Python：二级水平，函数薄弱，只能写简单脚本，阅读代码尚可；
>
> Linux：只会少量基础命令；
>
> 渗透：工具会用一点，原理理解很浅。

**Git 要不要学？要学，但不是放在最前面。**

CTF 做题本身**不考 Git 操作**，但是：

1. 网上靶场、exp、脚本、工具全部在 GitHub；
2. 以后你自己写脚本、保存 WP、做项目、复现漏洞，必须 Git；
3. 学习周期：**有效学习 6‑10 小时就够用 CTF 日常，不需要精通**，只掌握高频命令即可，不要深挖底层。

> Git 学习目标（对你够用版）：
>
> 克隆仓库、拉取更新、简单提交、上传自己脚本；不用学复杂分支、合并冲突这些高级内容。

------

## 整体学习路线（适配你的基础，分阶段，明确周期、做什么、不做什么）

> 总前提：**不要同时堆一大堆新东西，一次主攻 1‑2 个，其余维持复习。**
>
> 你现在最大问题：知识点广，但是每一块都很浅，缺少 “写代码” 的实操，做题看得懂，自己写不出来。

### 阶段 1：夯实编程基础（4‑6 周，每天 1.5‑2h）

> 目标：解决 “看得懂，写不出”，这是你现在最大瓶颈。

### Python（优先级最高）

你的短板：**函数、参数、循环逻辑、简单文件处理、网络请求**

不要刷很厚的书。

练习重点：

1. 函数定义、调用、参数、返回值（重中之重，你薄弱点）
2. 文件读写、字符串处理、正则 re 模块（CTF 高频）
3. requests 库发送 http 请求（web 题、exp 必备）
4. 写小脚本：抓网页、简单解码、批量处理字符串

练习方式：

- 不要搞复杂项目，**写几十段小脚本**，5‑30 行那种。
- CTF 简单 Python 题，先看懂，合上答案自己默写出来。

> 不要学：面向对象高级、GUI、复杂第三方库，现阶段完全用不上。

### Web 前端（HTML / CSS / JS）（3‑4 周穿插学）

目标：看懂网页源码，看懂简单 JS 逻辑，能读懂 JS 审计题，**不是成为前端工程师**。

- HTML：标签、表单、input、提交、get/post 参数
- CSS：只需要看懂，不用精通写漂亮页面
- JS 重点：变量、函数、字符串、eval、url 编码解码、简单 DOM，**XSS 题目大量依赖 JS 基础**

> 不需要学 Vue、框架，CTF web 几乎用不到。

### PHP（CTF 向，不要完整学网站开发）

只学 CTF 高频语法：

变量、数组、字符串函数、`$_GET/$_POST`、弱比较 `==`、文件包含、`eval`、`file_get_contents`，命令执行相关函数。

> 不用学 MVC、数据库完整项目，只学漏洞相关语法。

### Linux 基础（贯穿全程，每天 10‑15 分钟）

必掌握：cd ls pwd cat cp mv rm chmod vim 简单使用，curl，wget，管道 |，简单 grep。

> 不用深入内核、服务搭建，先把高频命令练熟。

### Git 学习安排（阶段 1 中后期开始，6‑10 小时总投入）

只学实用部分：

plaintext







```
git clone
git status
git add
git commit
git push
git pull
```

会下载别人项目，会上传自己写的 exp 脚本到 GitHub 就够。

> 不要花大量时间研究分支、rebase。

------

## 阶段 2：CTF 专项刷题训练（阶段 1 之后，长期持续）

> 你现在不适合直接猛刷难题，会很挫败。

### Web 方向（最适合你上手）

1. 先做简单题型：文件包含、命令执行、简单 XSS、PHP 弱类型、SQL 注入基础。
2. 平台推荐：

- WebGoat（基础漏洞原理）
- HackTheBox Starting point / 攻防世界 web 入门题
- 本地搭建 DVWA 靶场，逐个漏洞过一遍，看懂原理，尝试自己写简单 exp。

> 刷题正确模式：

1. 做不出来，看 WP，**不要复制粘贴完就过**
2. 把 WP 代码看懂，合上，自己手写一遍
3. 记录：这个漏洞原理是什么，关键函数是什么，payload 为什么这么写。

> 只复制不思考，刷再多题也记不住。

### 工具使用：burpsuite、xray，不要只会点按钮

每用一个工具，要搞懂：它在干什么，输出的信息代表什么，而不是一通点点点。

## 阶段 3：渗透实战（有基础之后再加大力度）

现在你的编程底子偏弱，渗透容易变成只会跑工具，看不懂返回结果。

先把编程基础提上来，再大量搞实战。

------

## 关于 “怎么有效记住题目”

你会觉得记不住，根源两个：

1. 底层语法不熟，题目背后的代码逻辑看不懂；
2. 只看答案，没有手动复现。

**记忆方法：**

1. 做完一道题，整理最小复现代码，保存到自己 GitHub。

> 比如 PHP 弱类型，就写一个 5 行的小 php 文件，本地跑一遍，看输出结果。

1. 建立错题本：记录：漏洞点、关键函数、payload、踩坑点。
2. 隔 3‑5 天回头重新做一遍之前做过的题，不看 WP。

> CTF 不是背题，是记住**漏洞原理 + 对应函数**，题目千变万化，底层就那套。

> ❌错误做法：疯狂刷大量题，做完就扔。
>
> ✅正确做法：少量题，吃透，复现，隔段时间复盘。

------

## 时间参考预估（每天有效学习 1.5‑2h）

1. Python 补齐函数 + web 基础：**4‑6 周**
2. Git 够用：**6‑10 小时分散到几周，不用突击**
3. 完成基础之后，CTF 入门 web 题型能独立解决简单题：再加 4‑6 周。

> 总计：从你现在水平，到能独立打普通 CTF 比赛拿一点分数，**3‑4 个月合理周期**。网络安全是慢功夫，不要指望一两周突飞猛进。

------

## 给你的每日简单执行模板，可以直接照抄执行

1. 40min：Python 练习（函数、脚本编写）
2. 30‑40min：Web/PHP/JS 基础或者靶场做题
3. 15‑20min：Linux 命令练习
4. 剩余时间：复盘昨天做过的题目，复现漏洞

> 每周抽 1‑2 次，学习 Git，练习克隆、提交脚本。